{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "cross-env NUXT_APP_BASE_URL=/homePage/ nuxt generate && npx gh-pages -d dist"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "nuxt": "^3.17.3",
    "sass": "^1.89.0",
    "sass-loader": "^13.3.3",
    "vue": "^3.5.14",
    "vue-router": "^4.5.1",
    "vuetify": "^3.8.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
